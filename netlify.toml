[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Next 16 requires Node >= 20.9; pin it so Netlify doesn't fall back to an older default.
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
